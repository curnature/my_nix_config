{
    # stores system type, username, network hostname, nix vserion, home-manager version
    # this is just a temporary file
    # may removed later when I figure out a better way to transfer variables between nix files
    system = "x86_64-linux";
    username = "curvature";
    hostname = "nixnoob";
    nixVersion = "25.05";
    homeVersion = "25.05";
}
